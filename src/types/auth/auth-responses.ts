export interface CheckEmailResponse {
  exists: boolean;
  message?: string;
}

export interface CheckEmailResponse {
  exists: boolean;
  message?: string;
}

export interface AuthResponse {
  token: string;
}
export interface OAuthResponse {
  accessToken: string;
  refreshToken: string;
}
